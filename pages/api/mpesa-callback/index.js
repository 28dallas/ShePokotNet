// Folder-based callback route scaffold:
// POST /api/mpesa-callback
// Delegates to existing M-Pesa callback handler for now.
// Replace with dedicated Daraja validation + reconciliation flow later.
export { default } from '../mpesa/callback'
