// Placeholder: implement secure server-side Stripe integration
export default async function handler(req, res) {
  res.status(200).json({ ok: true, message: 'Stripe endpoint placeholder. Configure STRIPE_SECRET_KEY.' })
}
