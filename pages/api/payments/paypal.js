// Placeholder: implement PayPal server-side integration
export default async function handler(req, res) {
  res.status(200).json({ ok: true, message: 'PayPal endpoint placeholder. Configure PAYPAL_CLIENT_ID and SECRET.' })
}
