// Placeholder: implement M-Pesa integration (STK Push / C2B) server-side
export default async function handler(req, res) {
  res.status(200).json({ ok: true, message: 'M-Pesa endpoint placeholder. Add M-Pesa credentials.' })
}
